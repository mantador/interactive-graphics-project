export class Constants {
  static nBodies = 20;
  static dt = 1.0;
  static G = 0.05;
  static log = false;
  static minRadius = 10;
  static maxRadius = 30;
  static lightPosition = [0, 0, 0];
  static cameraPosition = [500, 500, 2000];
  static cameraTarget = [500, 500, 500];
}
